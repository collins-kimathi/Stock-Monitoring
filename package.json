{
  "name": "dekar-smartpos-inventory-system",
  "version": "1.0.0",
  "private": true,
  "description": "Dekar Cyber and Stationaries Limited SmartPOS + Inventory System",
  "scripts": {
    "dev": "npm.cmd run dev --prefix frontend",
    "server": "npm.cmd run dev --prefix server",
    "stop": "powershell -ExecutionPolicy Bypass -Command \"Get-NetTCPConnection -LocalPort 5173 -ErrorAction SilentlyContinue | Select-Object -ExpandProperty OwningProcess -Unique | ForEach-Object { Stop-Process -Id $_ -Force -ErrorAction SilentlyContinue }\"",
    "client": "npm.cmd run dev --prefix frontend",
    "build": "npm.cmd run build --prefix frontend"
  }
}
